# Example Schedule Configuration for Avtomatika Native Scheduler

[maintenance_task]
blueprint = "periodic_maintenance"
interval_seconds = 60
enabled = false

[maintenance_task.initial_data]
source = "scheduler"
task_type = "cleanup"